<Window x:Class="ECTViews.Stammdaten.AnfangssaldoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Anfangssaldo"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5"
        FontFamily="Segoe UI" FontSize="13">

    <StackPanel Margin="16">
        <TextBlock Text="{Binding Beschriftung}" TextWrapping="Wrap"
                   Margin="0,0,0,8" Foreground="#333"/>

        <TextBox x:Name="WertBox" MaxLength="12" Padding="4,3"
                 HorizontalContentAlignment="Right" Width="140"
                 HorizontalAlignment="Left"
                 Text="{Binding Wert, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Fehlertext: rot, verschwindet bei leerem Text -->
        <TextBlock Text="{Binding Fehler}" Foreground="#D32F2F"
                   FontSize="11" FontStyle="Italic" Margin="0,4,0,0"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="OK" MinWidth="100" Padding="14,5" Margin="0,0,8,0"
                    IsDefault="True" IsEnabled="{Binding IstGueltig}"
                    Click="OnOk"/>
            <Button Content="Abbrechen" MinWidth="100" Padding="14,5"
                    IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
