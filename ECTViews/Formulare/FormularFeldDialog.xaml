<Window x:Class="ECTViews.Formulare.FormularFeldDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Formularfeld bearbeiten"
        Width="530" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5"
        FontFamily="Segoe UI" FontSize="13">

    <!-- [Feld-Eigenschaften]
         Pendant des nativen CFormularfeld-Dialogs (IDD_FORMULARFELD):
         alle Attribute eines .ecf-Formularfelds. Der Aufrufer schreibt
         die Werte bei OK zurueck in die Definition und speichert. -->

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,8,6"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="4,3"/>
                <Setter Property="Margin" Value="0,0,0,6"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Padding" Value="4,3"/>
                <Setter Property="Margin" Value="0,0,0,6"/>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Margin" Value="0,2,0,6"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>

        <TextBlock Text="Feld-Id:"/>
        <TextBox Grid.Column="1" x:Name="IdBox" Width="90"
                 HorizontalAlignment="Left"/>

        <TextBlock Grid.Row="1" Text="Typ:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="TypBox" IsEditable="True"/>

        <TextBlock Grid.Row="2" Text="Beschreibung:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="NameBox"/>

        <TextBlock Grid.Row="3" Text="Erweiterung:"
                   ToolTip="Formel (Summe), Einstellungs-Key(s), Dokument-Key oder Freitext"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ErweiterungBox"/>

        <TextBlock Grid.Row="4" Text="Position:"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
            <TextBlock Text="Seite" Margin="0,0,4,6"/>
            <TextBox x:Name="SeiteBox" Width="46"/>
            <TextBlock Text="horizontal" Margin="10,0,4,6"/>
            <TextBox x:Name="HorizontalBox" Width="60"/>
            <TextBlock Text="vertikal" Margin="10,0,4,6"/>
            <TextBox x:Name="VertikalBox" Width="60"/>
        </StackPanel>

        <TextBlock Grid.Row="5" Text="Ausrichtung:"/>
        <ComboBox Grid.Row="5" Grid.Column="1" x:Name="AusrichtungBox"
                  Width="160" HorizontalAlignment="Left">
            <ComboBoxItem Content="rechtsbündig"/>
            <ComboBoxItem Content="linksbündig"/>
        </ComboBox>

        <TextBlock Grid.Row="6" Text="Betragsanteil:"/>
        <ComboBox Grid.Row="6" Grid.Column="1" x:Name="AnteilBox"
                  Width="160" HorizontalAlignment="Left">
            <ComboBoxItem Content="brutto"/>
            <ComboBoxItem Content="netto"/>
            <ComboBoxItem Content="mwst"/>
        </ComboBox>

        <TextBlock Grid.Row="7" Text="Darstellung:"/>
        <StackPanel Grid.Row="7" Grid.Column="1">
            <CheckBox x:Name="OhneNachkommaBox"
                      Content="ohne Nachkommaanteil (ganze Euro)"/>
            <CheckBox x:Name="NullwertBox"
                      Content="Nullwerte anzeigen (statt leer)"/>
            <CheckBox x:Name="VeraltetBox"
                      Content="Feld ist veraltet"/>
        </StackPanel>

        <TextBlock Grid.Row="8" Text="ELSTER-Feldname:"/>
        <TextBox Grid.Row="8" Grid.Column="1" x:Name="ElsterBox"/>

        <TextBlock Grid.Row="9" Grid.ColumnSpan="2" x:Name="FehlerText"
                   Foreground="#D32F2F" FontSize="11" FontStyle="Italic"
                   TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Speichern" MinWidth="100" Padding="14,5"
                    Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button Content="Abbrechen" MinWidth="100" Padding="14,5"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
