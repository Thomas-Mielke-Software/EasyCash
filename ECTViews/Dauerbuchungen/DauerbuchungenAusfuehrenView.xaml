<Window x:Class="ECTViews.Dauerbuchungen.DauerbuchungenAusfuehrenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dauerbuchungen ausführen"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5"
        FontFamily="Segoe UI" FontSize="13">

    <StackPanel Margin="16">
        <TextBlock TextWrapping="Wrap" Foreground="#333" Margin="0,0,0,10"
                   Text="Erzeugt aus den fälligen Dauerbuchungen reale Buchungen bis einschließlich zum angegebenen Monat."/>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Bis (Monat/Jahr) inkl.:" VerticalAlignment="Center"
                       Margin="0,0,10,0" Foreground="#333"
                       ToolTip="Monat/Jahr, bis zu dem die Dauerbuchungen ausgeführt werden"/>
            <TextBox x:Name="MonatBox" Width="36" MaxLength="2" Padding="3,2"
                     Text="{Binding MonatText, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="." VerticalAlignment="Center" Margin="3,0"/>
            <TextBox Width="56" MaxLength="4" Padding="3,2"
                     Text="{Binding JahrText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="Ausführen" MinWidth="100" Padding="14,5" Margin="0,0,8,0"
                    IsDefault="True" Click="OnAusfuehren"/>
            <Button Content="Schließen" MinWidth="100" Padding="14,5" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
